# Nuxt resolves a lot of transitive tooling (Vite plugins, PostCSS, the b24ui preset) by bare
# specifier, so the dependency tree has to be flat for it.
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
ignore-workspace-root-check=true
