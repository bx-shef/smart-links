# Карта проекта SmartLinks

> Last reviewed: 2026-07-28

Навигатор по проекту «Умные ссылки»: что это, где мы сейчас, чего не хватает относительно
эталонного репозитория и в каком порядке приводим проект в порядок. Как работает само
приложение — в [`PROCESS.md`](PROCESS.md).

## Назначение

Bitrix24-приложение, которое добавляет пользовательский тип поля `SmartLink` и рендерит в
карточке CRM «умную ссылку» на связанную сущность (сделка или элемент Списка) с поиском,
привязкой, созданием и отвязкой. Nuxt 4 + Nitro: один процесс отдаёт лендинг, in-portal-страницы
и `/api/*`. Издатель — ИП Шевчик И.С.

## Эталон

Правила ведения проекта, структура документации и CI взяты из родственного репозитория
**`ai-price-import`** (AI-импорт прайсов, тот же издатель и стек Nuxt + Bitrix24). Там эти
вещи «сделаны правильно» — мы приводим SmartLinks к тому же порядку. Эталон **только для
чтения** (в нём ничего не правим).

## Где мы сейчас

Ядро продукта работает; идёт серверная миграция к паритету эталона:

- ✅ Регистрация UF-типа при установке, рендер поля в карточке, поиск/привязка/открытие/
  отвязка целевой сущности (CRM и Списки), реалтайм через pull, обратная связь через
  CRM-форму, i18n (ru).
- ✅ **Страница настроек поля** (`slider/app-options`) — рабочий редактор `UfSmartLinkType`
  (b24ui-форма: `FormField`+`Input`/`InputNumber`/`RadioGroup`/`Switch`/`Textarea`, валидация,
  доп. фильтр JSON). Конфиг грузится из опций (`initFromBatch`) и сохраняется через настройки +
  pull `reload.options`.
- ✅ **Захардкоженный конфиг портала убран** — `configUfListSettings` по умолчанию пустой,
  наполняется из опций / через страницу настроек.
- 🚧 Незакрытые `@todo`: фильтры `myCompany`/`dogovor`, «Uf для смарт-процессов»
  (есть в i18n, не подключено), шаг `demo` установки — плейсхолдер.
- ✅ Каркас, правила (`CLAUDE.md`), документация, CI и первые тесты (Vitest, unit) — готовы
  (см. [`ROADMAP.md`](ROADMAP.md) этапы 1–3).

## Чего не хватает относительно эталона (`ai-price-import`)

Легенда статусов: `TODO` не начато · `WIP` в работе · `DONE` сделано · `N/A` намеренно не
переносим.

| # | Что | Зачем | Приоритет | Статус |
|---|-----|-------|-----------|--------|
| G1 | `CLAUDE.md` — правила/конвенции/workflow | Единая памятка агенту и людям | P1 | DONE |
| G2 | Структура `docs/` + индекс + заголовки `Last reviewed` | Документация как в эталоне | P1 | DONE |
| G3 | CI (`.github/workflows/ci.yml`): lint + typecheck + test + build | Барьер регрессий | P1 | DONE |
| G4 | `.github/dependabot.yml` (npm root + github-actions) | Обновления зависимостей | P2 | DONE |
| G5 | `.claude/` хуки (session-start) | Готовность web-сессий с первого хода | P2 | DONE |
| G6 | `.gitignore` (полный, как в эталоне) | Гигиена репо | P2 | DONE |
| G7 | Тесты + Vitest | В эталоне чистое ядро под тестами | P1 | WIP (unit-проект есть; `nuxt`-проект — далее) |
| G8 | Конвенция «EN-комментарии / RU-доки», чистые функции в `utils` (+тесты) | Единый стиль кода | P2 | WIP |
| G9 | Нормальный `README.md` | Точка входа | P2 | DONE |
| G10 | Расширенный `.env.example` с комментариями | Понятная настройка | P3 | WIP |
| G11 | Документ по деплою | Воспроизводимый релиз | P2 | DONE кодом (`docs/DEPLOY_VIBECODE.md` + скрипт + opt-in workflow); ⚠ на живой платформе не прогонялось |
| G12 | (Опц.) CI-джоба: собирать архив B24 как артефакт/релиз | Готовый архив из CI | P3 | TODO |

### Что переносим из эталона дополнительно (по решению владельца)

Кроме каркаса, из эталона портируем продуктовые вещи (см. [`ROADMAP.md`](ROADMAP.md)):

- **Лендинг** (маркетинговая страница) ✅ — на `/`, пререндерится в реальный HTML (индексируем).
- **Попап «оцените приложение»** (`AppRatingModal`) ✅ — **серверный**, как эталон: решение показа
  на сервере (`/api/app-rating`, фрейм-токен, Postgres `app_rating`, чистая `shouldPrompt`),
  клиент только показывает и штампует `prompted`/`opened`, открывает Маркет через
  `slider.openPath`. Инертен без БД, вне портала и при пустом `NUXT_PUBLIC_B24_MARKET_CODE`.
- **Деплой в Bitrix24 Vibecode Black Hole** — [`docs/DEPLOY_VIBECODE.md`](DEPLOY_VIBECODE.md) (фаза S3).

✅ **Архитектурная развилка решена** (владелец, 2026-07-26): вариант «а» — **серверный процесс**
(Nitro), паритет с эталоном. Один процесс отдаёт лендинг `/`, in-portal-страницы и `/api/*`;
состояние рейтинга — в Postgres. Ход миграции — [`SERVER_MIGRATION.md`](SERVER_MIGRATION.md).

### Что пока НЕ переносим (`N/A`)

Части эталона под задачи AI-импорта: очереди BullMQ, LLM-экстрактор, OpenTelemetry, операторская
консоль. Postgres и фрейм-токен-авторизация **уже портированы** (фаза S2); OAuth-хранилище
токенов приедет с фазой S4 (облачное Market-приложение).

## План работ (актуальный)

1. ✅ Каркас проекта, чистка следов исходного портала, конвенции и первые тесты.
2. ✅ Продукт: рабочая страница настроек поля, конфиг из опций, захардкоженный конфиг убран.
3. ✅ Серверная миграция S1–S2 + публичный лендинг с пререндером (см. `SERVER_MIGRATION.md`).
3.1 ✅ **S3 — деплой в Black Hole** (G11): `DEPLOY_VIBECODE.md`, скрипт, opt-in workflow,
   edge-security (CSP/`frame-ancestors`, кап тела), rate-limit `/api/app-rating`, SSRF-allowlist
   зон. ⚠ **Живой прогон не делался** — первый деплой вручную по чек-листу.
4. **Срез CSS дизайн-системы для лендинга** — сейчас на публичную текстовую страницу грузится
   368 КБ CSS b24ui (≈40 КБ gzip), из них ~134 КБ — токены air-дизайна. План: свой вход Tailwind
   (`source(none)` + `@source` на `pages/index.vue`) для лендинга, b24ui-CSS — только на
   in-portal-маршрутах и `error.vue`. — *следующее.*
4.1 ✅ **S4 — Market OAuth**: вебхук `/api/b24/events`, `portal_tokens` с шифрованным
   `refresh_token`, привязка `member_id` к гранту, тумбстоун порядка событий, keep-alive рефреш;
   ключ рейтинга — `member_id` с фолбэком на хост. ⚠ **Живой прогон не делался.**
6. Дальше: пикеры полей в настройках, `nuxt`-проект Vitest, `@todo` (`myCompany`/`dogovor`,
   смарт-процессы), при необходимости — мультиязычность.

## Открытые продуктовые вопросы

- **Настройки поля**: базовый редактор готов. Дальше — пикеры полей/сущностей вместо ручного
  ввода кодов (async-поиск, как в эталоне), фильтры `myCompany`/`dogovor` (пока `@todo` в
  обработчике).
- **Проверка на живом портале**: сквозной round-trip настроек (`app.option.set` под `{options}`
  с JSON-строкой → чтение через `getJsonObject` при загрузке) реализован по контракту
  `@bitrix24/b24jssdk` (опции портала хранятся строками). Требует подтверждения на живом портале
  (REST-факт, а не по памяти).
- **Мультиязычность**: сейчас только `ru`. Нужны ли `be`/`kk`/`en` (в эталоне мультиязык).
- **Тариф/маркет**: статус приложения (`Free`/`Trial`) читается, но логики тарифа нет.

## Технические заметки (миграция b24ui)

Приложение на `@bitrix24/b24ui-nuxt` v1. В актуальной доке b24ui (v2+) часть используемых
компонентов помечена deprecated/переименована — учесть при апгрейде:

- `B24SidebarLayout` (все layout'ы + `app-options`) — deprecated, в 3.0.0 удаляется, замена —
  `DashboardSidebar`. Также `B24NavbarSection`/`B24NavbarSpacer` (`layouts/default.vue`) — из
  устаревшего layout.
- `B24ButtonGroup` (`handler/uf.smart-link`) — в доке v2 отдельного компонента нет; при апгрейде
  сверить с живым b24ui (возможная замена — `B24FieldGroup` или flex-обёртка).

Сейчас всё это валидно в установленной v1 (сборка/lint/typecheck зелёные) — заметка на будущий
апгрейд, не блокер.

## Конвенции (принимаем как в эталоне)

- Комментарии/JSDoc — английский; пользовательский текст и документация — русский.
- Каждый `.md` в корне и `docs/` несёт `> Last reviewed: YYYY-MM-DD` под H1.
- Чистые функции — в `app/utils/*` с тестами; данные/типы — отдельно от UI.
- Данные из API — только через `{{ }}` (auto-escape), без `v-html` с внешними данными.
- В `main` не пушим — только через PR; ветку берём из контекста сессии.
